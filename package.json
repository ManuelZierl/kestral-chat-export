{
  "name": "kestral-chat-export",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19 <23"
  },
  "scripts": {
    "build": "node build.mjs",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "npm run build && vitest run && node --test test/*.test.mjs",
    "test:package-digest": "node --test test/package-digest.test.mjs",
    "test:package-schema": "node test/package-schema.mjs",
    "test:reproducible": "node test/reproducible.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "@types/node": "^22.15.30",
    "esbuild": "0.25.6",
    "jsdom": "^26.1.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.2.2",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
